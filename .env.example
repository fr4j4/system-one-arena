COMBAT_DATA_DIR=./data/combat-v2
# Optional per-model profiles (credentials are environment references only).
COMBAT_PROFILES_FILE=providers.local.json
# Download and load weights only when explicitly enabled.
LAYA_ENABLED=false
LAYA_DEVICE=cpu
LAYA_CHECKPOINT=multilingual
TYPESAFE_API_KEY=
JEV_MODEL=jev-latest
# Optional chat-completions-compatible provider with JSON output.
GENERIC_BASE_URL=
GENERIC_API_KEY=
GENERIC_MODEL=
